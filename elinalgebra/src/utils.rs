// use core::str::from_utf8;
// use micromath::F32Ext;

// fn f32_to_str(mut f: f32, decimals: i32) -> &str {
// 	let bytes: [40; u8] = [40; 0];
// 	let mut byte_ptr = 0;
// 	for (i in 0..decimals) {
// 		let floor_f = f.floor();
// 		let digit = (f - floor_f) as i32;
// 		if (digit >= 10) {

// 		}
// 		f *= 10.0;
// 	}
// }

// fn i32_to_str(mut i: i32) {

// }
